# Implementer Brief

Use for an independently delegated task.

- Workspace and file ownership: [paths]
- Required behavior and acceptance criteria: [requirements]
- Relevant context and dependencies: [references]
- Verification and independent expected results: [checks]
- Constraints and existing decisions: [constraints]

Implement the task, resolve routine choices from the code and requirements, and ask the controller only about consequential missing information. Do not expand scope or edit another worker's files. Commit only if instructed.

Run focused checks for your change and self-check the diff. Report changed files, observed results, and unresolved concerns. Reuse available execution evidence; a second hand-written report is optional. Do not spawn reviewers or create a per-task review loop: the controller arranges the integrated project's independent quality-check.
