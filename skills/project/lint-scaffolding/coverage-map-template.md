# Lint カバレッジマップ（採否台帳）

本書はプロダクトの**決定的チェック（Lint / 静的解析）の採否台帳**である。カタログ（`documents/development/static-check-standard.md`）の25カテゴリすべてについて「どの手段で担保するか / なぜ採用しないか」を記録し、**カテゴリを採用しないことでチェックが黙って消えることを防ぐ**。不採用カテゴリは「AI レビュー担保」に割り当ててレビューガイド側に観点として残し、担保の所在を常にこの台帳から辿れるようにする。作成・更新は `lint-scaffolding` スキルが行い、`quality-check` のペルソナレビューは本台帳で Lint 担保に割当済みの項目をレビュー対象外とする。

- プロダクト名: <!-- 記入 -->
- 検出スタック: <!-- Step 1 の検出結果（例: Next.js + Prisma） -->
- 最終更新日: <!-- YYYY-MM-DD -->

## カバレッジマップ

固定列（番号 / カテゴリ / 採用基準 / 🤖）はカタログ §2 からの転記であり、書き換えない。記入するのは右3列である。

- **担保手段**: 配線した具体の資産・ツール参照（例: `lint/eslint/harness.config.mjs` の correctness グループ、`lint/ast-grep/security/`、`.ai-dev-helm-lint.json` の secrets チェック）。AI レビュー担保の場合は対応するレビューガイドの節を書く
- **採用状況**: `採用` / `不採用` / `AI レビュー担保` のいずれか。機械チェック可能なのに Lint を配線しない選択は `不採用` ではなく `AI レビュー担保` とする。`不採用` は該当技術がない等、チェック対象自体が存在しない場合に限る
- **理由・参照**: 採否の理由。生成ルールの場合は検証（違反 / 適合サンプルへの実実行）の事実も書く

| 番号 | カテゴリ | 採用基準 | 🤖 | 担保手段 | 採用状況 | 理由・参照 |
|---|---|---|---|---|---|---|
| A1 | 禁止パターン | 必須（全プロダクト） | - | | | |
| A2 | 非同期・Promise の正しさ | 必須（全プロダクト） | 🤖 | | | |
| A3 | エラーハンドリング規律 | 必須（全プロダクト） | 🤖 | | | |
| A4 | リソース管理 | 推奨（該当技術があるプロダクト） | - | | | |
| A5 | null・型安全 | 推奨（該当技術があるプロダクト） | - | | | |
| A6 | 数値・日時・通貨の精度 | 任意（プロダクト判断） | - | | | |
| A7 | 網羅性 | 推奨（該当技術があるプロダクト） | - | | | |
| B1 | シークレット・機密 | 必須（全プロダクト） | 🤖 | | | |
| B2 | コードレベル脆弱性パターン | 必須（全プロダクト） | 🤖 | | | |
| B3 | 依存の健全性 | 必須（全プロダクト） | 🤖 | | | |
| C1 | レイヤー境界・依存方向 | 推奨（該当技術があるプロダクト） | - | | | |
| C2 | 循環依存 | 推奨（該当技術があるプロダクト） | - | | | |
| C3 | デッドコード | 必須（全プロダクト） | 🤖 | | | |
| C4 | 複雑度・サイズ | 推奨（該当技術があるプロダクト） | - | | | |
| C5 | 重複 | 任意（プロダクト判断） | - | | | |
| C6 | マジックナンバー・ハードコード値 | 推奨（該当技術があるプロダクト） | 🤖 | | | |
| C7 | 命名・構造 | 任意（プロダクト判断） | - | | | |
| C8 | コメント規律 | 必須（全プロダクト） | - | | | |
| D1 | クエリ品質 | 推奨（該当技術があるプロダクト） | - | | | |
| D2 | ブロッキング・非効率パターン | 推奨（該当技術があるプロダクト） | - | | | |
| D3 | フロントエンド性能 | 任意（プロダクト判断） | - | | | |
| E1 | テスト妥当性 | 必須（全プロダクト） | 🤖 | | | |
| E2 | テスト規律 | 必須（全プロダクト） | - | | | |
| F1 | アクセシビリティ | 任意（プロダクト判断）※フロントエンド（UI）を持つプロダクトでは推奨に格上げ | - | | | |
| F2 | ログ規律 | 推奨（該当技術があるプロダクト） | - | | | |

## ミューテーションテスト設定の記録

事前ビルド設定はフェーズ4で提供予定。それまでの選択を記録する。

| 項目 | 記録 |
|---|---|
| 選択（生成 / スキップ） | <!-- 記入 --> |
| 登録コマンド（生成時） | <!-- mutation:full / mutation:diff の実体 --> |
| 理由 | <!-- 記入 --> |

## 更新規律

- **ルールを追加・削除・不採用に変更するたびに本台帳を更新する。** 台帳と実配線（`lint:all` が実行する内容・レビューガイドの記載）を乖離させない
- **スタック変更時（FW・DB アクセス層の追加/変更、init 再実行による資産更新）は `lint-scaffolding` スキルを再実行し**、影響する行を見直す。決定済みで影響のない行は引き継ぐ
- **カテゴリ ID（A1〜F2）は安定識別子であり、再採番しない**（カタログ §1）。カタログにカテゴリが追加された場合は各グループの末尾行として追記し、廃止された場合も行を欠番として残して経緯を「理由・参照」に書く
- Lint 担保から AI レビュー担保へ切り替える（またはその逆の）変更は、レビューガイド（`.github/review-*.md`）の「Lint 担保済み項目（AI レビュー対象外）」節と `（Catalog: <番号>）` 付記も同時に更新する
